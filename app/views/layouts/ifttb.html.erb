<!DOCTYPE html>
<html data-theme="lofi">
  <head>
    <title>IFTTB</title>

    <meta
      name='viewport'
      content='minimum-scale=1, initial-scale=1, width=device-width, user-scalable=no, shrink-to-fit=no, viewport-fit=cover'
    />
    <meta name='theme-color' content='#000000'>

    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= favicon_pack_tag 'ifttb_favicon.png' %>
    <%= javascript_include_tag "ifttb", "data-turbo-track": "reload", defer: true %>
    <%= stylesheet_link_tag "ifttb", "data-turbo-track": "reload" %>
  </head>

  <body class="font-mono">
    <% if content_for? :navbar %>
      <%= yield :navbar %>
    <% end %>

    <% if content_for? :tabbar %>
      <div class="fixed bottom-0 z-10 w-full bg-base-100 text-white bg-neutral">
        <%= yield :tabbar %>
        <div style="height:env(safe-area-inset-bottom)"></div>
      </div>
    <% end %>

    <div class="mx-auto max-w-screen-lg">
      <%= yield %>
    </div>

    <%= turbo_frame_tag 'modal' %>
  </body>
</html>
