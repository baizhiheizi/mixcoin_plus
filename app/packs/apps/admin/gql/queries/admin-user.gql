query AdminUser($id: ID!) {
  adminUser(id: $id) {
    id
    name
    avatar
    mixinId
    mixinUuid
    invitationsCount
    broker {
      mixinUuid
    }
    invitor {
      name
      avatar
      mixinId
    }
    createdAt
  }
}
